<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <!-- 用最新的IE浏览 -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>相册</title>
    <link href="/css/bootstrap.css" rel="stylesheet">
    <style>    
      body{
        margin: 0;
        padding: 0;
        background: #f2f2f2;
      }
      .center{
        text-align: center;
      }
      form{
        width: 250px;
        margin: 50px auto 0;
      }
      select{
        width: 100px;
        height: 30px;
      }
      option{
        height: 30px;
        padding: 30px;
      }
      .center input{
        display: inline-block;
        width: 100%;
      }
    </style>
  </head>
  <body>
    <h3 class="center">上传到我的相册</h3>
    <p class="center"><a href="/">全部相册</a></p>

    <form action="" method="post" enctype="multipart/form-data">
      <p>
        请选择相册
        <select name="albumName">
          <% for( var i = 0 ; i < albums.length ; i++ ){ %>
            <option value="<%= albums[i] %>"><%= albums[i] %></option>
          <% } %>
        </select>
      </p>
      <p><input type="file" name="tupian" /></p>
      <p class="center"><input type="submit" /></p>
    </form>

    <script src="/js/jquery-1.9.1.min.js"></script>
    <script src="/js/bootstrap.js"></script>
    <script>
      $('select').on('change', function() {
        console.log( this.value );
      });
    </script>
  </body>
</html>